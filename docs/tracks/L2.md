# Track L2 — Multi-pair OLS mean reversion

**Status:** active  
**Owner:** Agent L2  
**Params prefix:** `mpairs_*` (default off)  
**Kill rule:** 3 consecutive non-promotable ticks → freeze  
**Floor:** production score **265.18** (post-L1 promote)

## Hypothesis bank
1. Top-k corr-screened pairs among instruments 1..50 (exclude ALGO), OLS hedge, z-entry.
2. Vary top_k ∈ {3,5,8}, min_corr ∈ {0.65,0.70,0.75} (≤ empirical ~0.77 at lb=40), lookback ∈ {40,60,80}.
3. Modest blend weights {0.10,0.20} so L2 does not dominate base reversal.

## Reject memory
- Tick 1: all mpairs overlays diluted production (best 221.94 < floor 251.70); scores collapsed by weight only (lb/k/z/corr inert). Primary root cause: `mpairs_min_corr` (0.85/0.90) above empirical max pairwise corr on this data (~0.77 at lb=40) — grid never selected pairs. Score collapse was pure dilution from the ungated blend (always mixing zero overlay into base); not merely entry_z. (Blend now gated when overlay inactive.)

## Log
| Tick | Hypothesis | Best score | train | half2 | Verdict |
|:---|:---|---:|---:|---:|:---|
| 1 | Corr-screened top-k pair OLS overlay on production floor (lb∈{40,60}×k∈{3,5}×w∈{0.10,0.20}×z∈{2.0,2.5}×cmin∈{0.85,0.90}) | 221.94 | 21.55 | 146.21 | promote false — HOLD floor (best mpairs=40@0.10k3z2.00c0.85; do not edit teamName.py) |
