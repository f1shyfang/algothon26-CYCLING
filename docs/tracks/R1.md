# Track R1 — Rolling OLS ALGO–basket

**Status:** parked for later ensemble research  
**Owner:** Agent R1  
**Params prefix:** `ols_*` (default off)  
**Kill rule:** 3 consecutive non-promotable ticks → freeze  
**Floor:** production is now minimal core + promoted R2 multi-pair overlay; `ols_*` remains off  
**Gates:** walk-forward F1–F3 majority + official ↑ + F3 ≥ 0.95×base F3

> Lead reconciliation parked R1 rather than rejecting it: its **218.81**
> promotable result was lower than R2's **221.91**. Retain it for a later
> ensemble experiment; no ensemble was attempted during this promotion.

## Hypothesis bank
1. Rolling OLS β on ALGO vs equal basket; z-entry mean-reversion overlay on minimal core.
2. Lookback ∈ {30,40,60} × weight ∈ {0.10,0.20,0.30} × entry_z ∈ {1.5,2.0,2.5}; intercept off.
3. Intercept-on OLS (`ols_intercept=True`) as a secondary arm after tick-1.

## Reject memory
(none yet — prior L1 promote was under old gates / old floor; re-discover)

## Log
| Tick | Hypothesis | Official | F1 | F2 | F3 | Verdict |
|:---|:---|---:|---:|---:|---:|:---|
| 1 | OLS ALGO–basket, lb=40, weight=0.30, entry z=2.0 (floor: 211.49 / 312.09 / 163.64 / 265.88) | 218.81 | 286.30 | 184.72 | 266.52 | PARKED — `promote: true`, but lower than R2; retain for later ensemble |
